<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flajutour - Viagens Personalizadas em São Paulo</title>
  
  <link rel="stylesheet" href="css/style.css">

  <!-- Hero-specific styles for homepage -->
  <style>
    /* Cabeçalho translúcido sobre a imagem */
    .site-header{
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      z-index: 40;
      background: rgba(255,255,255,0.08);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
      border-bottom: 1px solid rgba(0,0,0,0.06);
    }
    .site-header .header-inner{
      display:flex;
      align-items:center;
      gap:1rem;
      padding:0.6rem 1rem;
      flex-wrap: nowrap;
    }
    .site-header .logo{ height:44px; display:block; flex-shrink:0; }

    /* Nav principal (linha única) */
    .primary-nav {
      display:flex;
      align-items:center;
      gap:1rem;
      flex-wrap: nowrap;
      white-space: nowrap;
      margin-left: 1.25rem;
    }
    .primary-nav a {
      color: #fff;
      text-decoration: none;
      font-weight:600;
      text-shadow: 0 1px 2px rgba(0,0,0,0.6);
      padding: 6px 8px;
      flex-shrink:0;
    }

    /* botão login na linha principal */
    .btn-login {
      background: #ff7a2d;
      color: #fff;
      padding: 8px 12px;
      border-radius: 8px;
      text-decoration: none;
      font-weight:700;
      box-shadow: 0 1px 0 rgba(0,0,0,0.15);
    }

    /* botão hamburger (agora sempre visível) */
    .hamburger {
      display: inline-flex; /* mostrar sempre */
      align-items: center;
      justify-content: center;
      width: 42px;
      height: 42px;
      border: 0;
      background: rgba(0,0,0,0.28);
      border-radius: 8px;
      cursor: pointer;
      flex-shrink: 0;
      margin-left: 8px;
      z-index: 90;
      position: relative;
    }
    .hamburger span,
    .hamburger span::before,
    .hamburger span::after {
      display: block;
      background: #fff;
      height: 2px;
      width: 18px;
      border-radius: 2px;
      position: relative;
    }
    .hamburger span::before,
    .hamburger span::after {
      content: "";
      position: absolute;
      left: 0;
    }
    .hamburger span::before { top: -6px; }
    .hamburger span::after  { top: 6px; }

    /* Menu do hamburger (itens secundários) */
    .mobile-nav {
      display: none;
      position: absolute;
      top: 64px;
      right: 12px;
      background: rgba(0,0,0,0.92);
      padding: 0.8rem;
      border-radius: 8px;
      flex-direction: column;
      gap: .6rem;
      min-width: 220px;
      z-index: 95;
      box-shadow: 0 6px 18px rgba(0,0,0,0.35);
    }
    .mobile-nav.open { display: flex; }
    .mobile-nav a {
      color: #fff;
      text-decoration:none;
      padding: 8px 10px;
      border-radius:6px;
      display:block;
    }
    .mobile-nav a:hover { background: rgba(255,255,255,0.04); }

    /* estilo hero e restante (mantidos) */
    .hero {
      background-image: linear-gradient(rgba(0,0,0,0.46), rgba(0,0,0,0.32)), url('assets/capanovasite.png');
      background-size: cover;
      background-position: center bottom;
      background-repeat: no-repeat;
      min-height: 75vh;
      display: flex;
      align-items: flex-end;
      color: #fff;
      padding-top: 72px;
    }

    .hero .hero-inner {
      padding: 2.5rem 1rem 4.5rem;
      max-width: 1100px;
      margin: 0 auto;
      text-align: center;
      width: 100%;
    }

    .hero .tag { background: rgba(255,255,255,0.12); color:#fff; padding:.35rem .7rem; border-radius:999px; margin-bottom:1rem; }

    .hero h1, .hero p {
      color: #fff;
      -webkit-text-stroke: 0.6px rgba(0,0,0,0.95);
      text-shadow: 0 1px 2px rgba(0,0,0,0.65);
      margin: 0;
    }

    .hero h1 {
      font-size: clamp(1.6rem, 4.5vw, 3.2rem);
      line-height: 1.05;
    }

    .hero p {
      font-size: clamp(1rem, 1.4vw, 1.15rem);
      margin-top: .8rem;
      opacity: .95;
    }

    /* responsividade */
    @media (max-width: 920px) {
      .primary-nav { display: none; } /* opcional: esconde nav principal em telas pequenas */
      .hero { min-height: 62vh; padding-top: 64px; align-items: center; }
      .site-header .header-inner{ padding:0.5rem 0.85rem; }
      .mobile-nav { top: 56px; right: 8px; }
    }
    @media (max-width: 480px) {
      .hero { min-height: 55vh; }
      .hero h1 { font-size: 1.6rem; }
      .mobile-nav { right: 8px; top: 56px; min-width: 180px; }
    }

    /* esconder scrollbar visual (opcional) */
    .main-nav::-webkit-scrollbar { height: 6px; }
    .main-nav::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.12); border-radius: 6px; }
  </style>

  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-PFEP9B1ERC"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-PFEP9B1ERC');
  </script>
</head>
<body>
  <div id="header-placeholder"></div>

  <main>
    <section id="home" class="hero">
      <div class="hero-inner">
        <span class="tag">Turismo Receptivo em São Paulo</span>
        <h1 data-translate="heroTitle">São Paulo te espera. Nós te guiamos.</h1>
        <p data-translate="heroSubtitle">Especialistas em turismo de empresarial, cultura, gastronomia e healt and care em São Paulo. Nossa cidade, sua experiência.</p>
      </div>
    </section>

    <section id="servicos" class="section container">
      <h2 data-translate="servicesTitle">Nossos Serviços</h2>
      <div class="grid-3">
        <div class="service">
          <h3 data-translate="eventTitle">Turismo de Eventos</h3>
          <p data-translate="eventDesc">Cuidamos de toda a logística da sua participação em eventos e feiras, para que você se concentre apenas no que realmente importa.</p>
        </div>
        <div class="service">
          <!-- removido termo "Logística para Bem-Estar" — título ajustado -->
          <h3 data-translate="healthAndCareTitle">Logística para Health and Care</h3>
          <p data-translate="healthAndCareDesc">Oferecemos suporte completo e humanizado para sua estadia, cuidando de toda a logística necessária para quem busca serviços de saude e bem estar na cidade.</p>
        </div>
        <div class="service">
          <h3 data-translate="culturalTitle">Turismo Cultural</h3>
          <p data-translate="culturalDesc">Descubra a história, a arte e os sabores de São Paulo em roteiros feitos sob medida para a sua paixão.</p>
        </div>
      </div>
    </section>
    
    <section id="quemsomos" class="section container secao-quem-somos">
      <h2 data-translate="aboutTitle">Quem Somos</h2>
      <div>
        <p data-translate="aboutDesc" style="text-align: center; max-width: 720px; margin: 0 auto 2.5rem;">A Flajutour nasceu do encontro de duas paixões complementares: cultura e movimento. Nossa missão é oferecer uma experiência de turismo receptivo em São Paulo com excelência, segurança e um toque pessoal que só quem conhece a cidade de verdade pode proporcionar.</p>
        <div class="grid-socios">
          <div class="socio-card">
            <img src="assets/flavia.png" alt="Foto de Flávia Cristina">
            <h3 data-translate="flaviaName">Flávia Cristina</h3>
            <p data-translate="flaviaBio">Psicóloga e guia de turismo, apaixonada por arte e cultura. Flávia é a mente por trás dos roteiros exclusivos, garantindo que cada cliente tenha uma experiência rica e inclusiva em São Paulo.</p>
          </div>
          <div class="socio-card">
            <img src="assets/jc.png" alt="Foto de Julio Cesar">
            <h3 data-translate="julioName">Julio Cesar</h3>
            <p data-translate="julioBio">Consultor de marketing e estudante de turismo, conhece São Paulo como a palma de sua mão. Com anos de experiência em logística e mobilidade urbana, Julio garante que cada trajeto seja seguro, eficiente e sem imprevistos.</p>
          </div>
        </div>
      </div>
    </section>

    <section id="contato" class="section container">
      <h2 data-translate="contactTitle">Reservas & Orçamentos</h2>
      <p data-translate="contactSubtitle" style="text-align: center; max-width: 600px; margin: -1rem auto 2rem;">Preencha o formulário e nossa equipe entrará em contato em breve para montar um pacote sob medida para você.</p>
      <form id="contactForm">
  <div class="form-grid">
    <input type="text" name="nome" data-translate="formName" placeholder="Seu nome" required>
    <input type="email" name="email" data-translate="formEmail" placeholder="Seu e-mail" required>
  </div>
  <div class="form-grid">
    <input type="tel" name="telefone" data-translate="formPhone" placeholder="Seu telefone (com DDD)" required>
    <div class="field-group">
      <label data-translate="hasWhatsappLabel">Possui WhatsApp?</label>
      <div class="radio-group">
        <label><input type="radio" name="whatsapp" value="sim" data-translate="whatsappYes" required> Sim</label>
        <label><input type="radio" name="whatsapp" value="nao" data-translate="whatsappNo"> Não</label>
      </div>
    </div>
  </div>
  <select name="cliente" data-translate="formClientType" required>
    <option value="" data-translate="selectClientType">Selecione o tipo de cliente</option>
    <option value="pf" data-translate="pf">Pessoa Física</option>
    <option value="pj" data-translate="pj">Empresa</option>
  </select>
  <div class="form-grid">
    <input type="number" name="adultos" placeholder="Número de adultos" min="1" required>
    <input type="number" name="criancas" placeholder="Número de crianças" min="0">
  </div>
  <select name="interesse" required>
    <option value="">Principal interesse na estadia</option>
    <option value="trabalho">Trabalho</option>
    <option value="diversao">Diversão</option>
    <option value="tratamento-saude">Tratamento de Saúde</option>
  </select>
  <select name="acomodacao" required>
    <option value="">Tipo de acomodação de preferência</option>
    <option value="hotel">Hotel</option>
    <option value="flat">Flat</option>
    <option value="airbnb">Airbnb</option>
  </select>
  <div class="form-grid">
    <input type="date" name="chegada" required>
    <input type="date" name="partida" required>
  </div>
  <textarea name="mensagem" data-translate="formMessage" placeholder="Observações adicionais" rows="5"></textarea>
  <button type="submit" class="btn-primary" data-translate="formSubmit">Enviar Pedido de Proposta</button>
</form>
    </section>
  </main>

  <footer class="site-footer">
    <p data-translate="footerCopyright">&copy; 2025 Flajutour. Todos os direitos reservados.</p>
    <p data-translate="footerContact">Contato: +55 (11) 2367-3909 | WhatsApp: +55 (11) 91129-3987</p>
    <p><a href="politica-de-privacidade.html" data-translate="privacyLink">Política de Privacidade</a></p>
  </footer>

  <script src="load-header.js"></script>
</body>
</html>

